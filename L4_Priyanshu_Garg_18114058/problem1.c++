#include <bits/stdc++.h>
using namespace std;

int main(){
    int N, n;
    cin >> N;
    int a[N];
    for(auto i : a)
        cin >> i;
    cin >> n;
    int sum = 0, len = 0;
    unordered_map<int, int> unmap;
    for(int i = 0; i < N; i++)
    

}